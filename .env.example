# Copy to .env and fill in your values
CODEX_PROXY_API_KEY=your-upstream-api-key-here
UPSTREAM_URL=http://host.docker.internal:3001/v1
# For OpenAI-compatible upstreams, use the API key they expect
# For Ollama/LM Studio (no auth), use a dummy key like: sk-local